-- 코드를 입력하세요
SELECT NAME, TEMP.COUNT
FROM (
    SELECT NAME, COUNT(NAME) COUNT
    FROM ANIMAL_INS
    WHERE NAME IS NOT NULL
    GROUP BY NAME
) AS TEMP
WHERE TEMP.COUNT >=2
GROUP BY NAME
ORDER BY NAME;